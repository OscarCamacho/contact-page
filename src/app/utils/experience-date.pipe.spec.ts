import { ExperienceDatePipe } from './experience-date.pipe';

describe('ExperienceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
